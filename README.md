```
usage: download_images.py [-h] [-p PATH] [-f | -r] filename

positional arguments:
  filename              file containing list of image URLs to download

optional arguments:
  -h, --help            show this help message and exit
  -p PATH, --path PATH  path to store downloaded images in
  -f, --force           overwrite existing files
  -r, --rename          rename existing downloaded file if file already exists
```
